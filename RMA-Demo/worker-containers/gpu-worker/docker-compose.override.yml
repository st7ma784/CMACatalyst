version: '3.8'

# Override configuration for production deployment
# This file overrides docker-compose.yml with production settings

services:
  gpu-worker:
    environment:
      - COORDINATOR_URL=https://api.rmatool.org.uk
      - USE_TUNNEL=true
      - SERVICE_PORT=8000
      - SERVICE_NAME=vllm-service
      - NVIDIA_VISIBLE_DEVICES=all
