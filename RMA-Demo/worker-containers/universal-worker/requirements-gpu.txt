# GPU libraries (installed only if GPU capability needed)
# vLLM is NOT included - installed at runtime if llm-inference service assigned
torch>=2.1.0
transformers>=4.35.0
accelerate>=0.25.0
bitsandbytes>=0.41.0
