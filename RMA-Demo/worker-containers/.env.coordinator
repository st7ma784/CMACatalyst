# Coordinator URL for workers
# Update this when deploying coordinator to different platforms

# Cloudflare (ACTIVE - auto-deployed via Cloudflare Pages/Workers)
COORDINATOR_URL=https://api.rmatool.org.uk

# Render.com (deprecated)
# COORDINATOR_URL=https://api.rmatool.org.uk

# Railway (alternative)
# COORDINATOR_URL=https://rma-coordinator-production.up.railway.app

# Fly.io (deprecated)
# COORDINATOR_URL=https://api.rmatool.org.uk

# Local development
# COORDINATOR_URL=http://rma-coordinator-local:8080

# Local testing (host network)
# COORDINATOR_URL=http://localhost:8080
